# 에이전트 서버 실행에 필요한 Python 패키지 목록입니다.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
requests>=2.31.0
python-multipart>=0.0.9
pillow>=10.0.0
diffq>=0.0.0
# GPU가 있는 시스템에서는 PyPI가 가능한 경우 GPU 빌드를 자동으로 선택합니다.
# CPU 전용 빌드가 필요하면 `torch==2.12.0+cpu`와 `torchaudio==2.11.0+cpu`를 사용하세요.
torch>=2.12.0
torchaudio>=2.11.0
# 실행 파일 패키징(PyInstaller)용 — 배포 빌드 시에만 필요할 수 있습니다.
pyinstaller>=6.0.0
