# OG 업로드 폴더에서 스크립트 실행 금지
<FilesMatch "\.(php|phtml|phar|pl|cgi|exe)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
